function palindrome(x, x1, x2, x3) {
  Math.abs(x);
  Math.ceil(x);
  Math.floor(x);
  Math.round(x);
  Math.max(x1, x2, x3);
  Math.min(x1, x2, x3);
  Math.random();
}

function palindrome(x1,x2,x3,x4) {
    // Método Math.abs()
    console.log("Math.abs(x):", Math.abs(x1));

    // Método Math.ceil()
    console.log("Math.ceil(x1):", Math.ceil(x1));

    // Método Math.floor()
    console.log("Math.floor(x2):", Math.floor(x2));

    // Método Math.round()
    console.log("Math.round(x3):", Math.round(x3));

    // Método Math.max()
    console.log("Math.max(x1,x2,x3,x4):", Math.max(x1,x2,x3,x4));

    // Método Math.min()
    console.log("Math.min(x1,x2,x3,x4):", Math.min(x1,x2,x3,x4));

    // Método Math.random()
    console.log("Math.random():", Math.random());

}

// Prueba de la función
palindrome(-5,4.3,4.9,7);

